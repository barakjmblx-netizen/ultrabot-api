Вот готовый `README.md` для вашего проекта **UltraBot API**, полностью соответствующий вашим требованиям:

---

# 🤖 UltraBot API — Продвинутый Minecraft Bot Framework  
*Без `mineflayer`. Полная поддержка новых версий. Offline-аккаунты. Swarm Intelligence. Imitation Learning.*

---

## 📁 Структура проекта

```
ваша-папка/
├── launcher.py             ← ✅ Создаёте ВЫ
├── bots/                   ← ✅ Создаёте ВЫ (кладёте своих ботов сюда)
│   └── example_bot.js      ← ✅ Пример бота (создаёте ВЫ)
└── ultrabot-api/           ← ❌ НЕ ТРОГАТЬ — уже создано
    ├── core/
    ├── modules/
    ├── ai/
    ├── tools/
    ├── config/
    ├── versions/           ← ✅ Автоматически создаётся API при первом запуске
    ├── templates/
    ├── index.js
    └── package.json
```

> ⚠️ **Важно**:  
> - **Не редактируйте** содержимое папки `ultrabot-api/` — всё уже настроено.  
> - **Создайте только 2 вещи**:  
>   1. `launcher.py` в корне  
>   2. папку `bots/` с вашими `.js` ботами внутри  

---

## ▶️ Как использовать

### Шаг 1. Установите зависимости
```bash
cd ultrabot-api
npm install
```

### Шаг 2. Создайте `launcher.py` в корне проекта

```python
# launcher.py
import os
import subprocess

# Настройки
API_DIR = "./ultrabot-api"
BOTS_DIR = "./bots"
HOST = "127.0.0.1"
PORT = 25565
VERSION = "1.21.11"        # ← любая версия из version.txt
PASSWORD = "123456789Q"
PREFIX = "data"

# Запуск всех ботов из папки bots/
for filename in os.listdir(BOTS_DIR):
    if filename.endswith(".js"):
        bot_name = filename.replace(".js", "")
        cmd = [
            "node",
            os.path.join(API_DIR, "index.js"),
            bot_name,
            HOST,
            str(PORT),
            VERSION,
            PASSWORD,
            PREFIX
        ]
        print(f"▶ Запуск бота: {bot_name}")
        subprocess.Popen(cmd, cwd=API_DIR)
```

### Шаг 3. Создайте `bots/example_bot.js`

```js
// bots/example_bot.js
module.exports = (bot) => {
  console.log(`[BOT ${bot.username}] Запущен на версии ${bot.version}`);
  
  // Пример: бот начинает ломать блоки
  bot.movement.startBreaking(2, 30);
  
  // Пример: отправка в чат
  bot.sendChat("Привет от UltraBot!");
};
```

### Шаг 4. Запустите
```bash
python launcher.py
```

---

## 🔧 Как работает управление версиями

- При первом запуске с **новой версией** (например, `1.21.11`), API:
  1. Проверяет файл `version.txt` (встроен в систему)
  2. Создаёт папку `ultrabot-api/versions/1.21.11/`
  3. Копирует шаблон из `ultrabot-api/templates/bot_template.js` в `versions/1.21.11/bot.js`
- **Ваши боты НЕ кладутся в `versions/`!**  
  Они всегда запускаются **из вашей папки `bots/`**, а `versions/` используется **только для внутренней совместимости**.

---

## ⚙️ Поддерживаемые версии

Все версии из вашего файла [`version.txt`](version.txt) поддерживаются, включая:
- `1.21.11`
- `1.21.10`
- `1.20.6`
- `1.19.4`
- `1.18.2`
- `1.16.5`
- и все промежуточные снапшоты (`25w46a`, `24w45a` и т.д.)

> Если вы укажете **несуществующую версию**, API выдаст ошибку и **не запустит бота**.

---

## 🧠 Возможности API

- ✅ **Авто-регистрация / логин** (`/register`, `/login`)
- ✅ **Реальное движение**: ходьба, бег, прыжки, плавание
- ✅ **Добыча, атака, стрельба из лука**
- ✅ **Чат-команды**: `Bot1 грызть 2 32`
- ✅ **Swarm Intelligence**: боты делятся знаниями
- ✅ **Imitation Learning**: обучение, наблюдая за игроками
- ✅ **Авто-переподключение** при отключении
- ✅ **Адаптация под версию**:  
  - `1.8` → `entity_action`  
  - `1.19+` → `chat_command`  
  - `1.21.2+` → новый формат registry

---

## ❓ Почему не работает?

1. **Боты не запускаются?**  
   → Убедитесь, что `launcher.py` и `bots/` лежат **рядом с `ultrabot-api/`**, а не внутри.

2. **Ошибка версии?**  
   → Используйте **точно такие же названия**, как в [`version.txt`](version.txt), например:  
   ✅ `"1.21.11"`  
   ❌ `"1.21.11 release"`  

3. **Боты не двигаются?**  
   → В `example_bot.js` вызовите `bot.movement.startBreaking()` или другое действие.

---

## 📜 Лицензия

MIT — делайте что угодно, но **оставьте авторство**.

---

> ✨ **UltraBot API — это не замена mineflayer. Это его эволюция.**
