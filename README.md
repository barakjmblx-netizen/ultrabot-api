

---

## 🚀 UltraCore API v5.0 — Next-Gen Minecraft Bot Framework

**UltraCore API** — это **мощный, гибкий и полностью независимый** фреймворк для создания **умных Minecraft-ботов** на **Node.js**.

> **Цель**: Предоставить **максимально полный функционал** для ботов без использования устаревших библиотек.

---

### ✨ Особенности

- **Полностью на `minecraft-protocol`** — без `mineflayer`
- **Поддержка новых версий** (1.8 — 1.21+)
- **Реальное движение** через пакеты (`steer_vehicle`, `entity_action`, `position_look`)
- **Автоматическая адаптация под версию**:
  - `1.8`: `chat` → `1.19+`: `chat_command`
  - `1.8`: `entity_action` для прыжка → `1.21+`: новые пакеты
- **ИИ-функции**:
  - **Swarm Intelligence**: обмен знаниями между ботами
  - **Imitation Learning**: обучение через наблюдение за игроками
  - **Распределённая память**: карты знаний, страхи, привычки
- **Чат-команды**: `Bot001 грызть 2 32`
- **Навигация по чанкам** (A* pathfinding без `mineflayer-pathfinder`)
- **Реальный крафт и добыча** через `window_click`
- **Синхронизация с хозяином** (через `stdin`, для интеграции с `pynput`)
- **Интеграция с внешними API**:
  - Minecraft Wiki (рецепты)
  - Ollama (генерация стратегий)
- **Автоматическое управление версиями**:
  - Парсинг `version.txt`
  - Создание папок `versions/<version>/`
  - Автоматическая проверка поддержки версии при запуске

---

### 📦 Структура проекта

```
ultrabot-api/
├── package.json
├── index.js                    # Точка входа
├── templates/
│   └── bot_template.js         # Шаблон для ботов
├── versions/                   # Папки под каждую версию
│   ├── 1.21.11/
│   │   └── bot.js              # Бот для 1.21.11
│   ├── 1.20.6/
│   │   └── bot.js              # Бот для 1.20.6
│   └── versions.json           # Список всех версий
├── core/
│   ├── BotEngine.js            # Главный движок
│   ├── VersionAdapter.js       # Адаптация под версию
│   ├── Movement.js             # Реальное движение
│   ├── WorldModel.js           # Модель мира
│   ├── BehaviorController.js   # Логика поведения
│   ├── CraftingSystem.js       # Система крафта
│   ├── SyncHandler.js          # Обработка команд stdin
│   └── modules/
│       └── ChunkNavigator.js   # Навигация по чанкам
├── ai/
│   ├── SwarmMemory.js          # Обмен знаниями
│   └── ImitationLearner.js     # Обучение через наблюдение
├── config/
│   └── global_settings.json    # Настройки
├── tools/
│   └── update_versions.js      # Обновление версий
└── data/                       # Временные данные
```

---

### 🛠️ Установка

1. Убедитесь, что у вас установлен **Node.js >= 18**.
2. Клонируйте или создайте проект.
3. Установите зависимости:

```bash
cd ultrabot-api
npm install
```

---

### ▶️ Запуск

Запуск бота:

```bash
node index.js <username> <host> <port> <version> [password] [prefix]
```

**Пример:**

```bash
node index.js Bot001 127.0.0.1 25565 1.21 mypass data
```

- `username`: Имя бота
- `host`: IP сервера
- `port`: Порт сервера
- `version`: Версия Minecraft (например, `1.21`, `1.20.6`)
- `password`: Пароль (если нужен для регистрации/логина)
- `prefix`: Папка для сохранения данных (`data` по умолчанию)

> **Важно**: Версия должна быть в списке поддерживаемых (см. `versions/versions.json` или `version.txt`).

---

### 🧠 ИИ-Функции

#### 1. Swarm Intelligence (Ройной интеллект)

- Боты **обмениваются знаниями** между собой (например, координаты алмазов).
- Используется **внутренний протокол** (не Minecraft-чат).
- Каждый бот хранит **карту знаний**.

#### 2. Imitation Learning (Обучение через подражание)

- Боты **наблюдают за игроками** и **запоминают** последовательности действий.
- Автоматически **создают "скрипты поведения"**.
- Применяют изученное в **похожих ситуациях**.

#### 3. Распределённая память

- Каждый бот имеет **свой опыт**, но **может делиться** с другими.
- Поддерживает **страхи**, **привычки**, **отношения**, **идеологии**.

---

### 🗣️ Чат-команды

Боты могут реагировать на команды в чате, отправленные в формате:

```
<username> <действие> <параметр1> <параметр2>
```

**Примеры:**

- `Bot001 грызть 2 32` — начать добычу с силой 2 и скоростью 32
- `Bot001 стоп действие` — остановить текущее действие

> Настройки команд находятся в `config/global_settings.json`.

---

### 🧩 Интеграция с внешними API

- **Minecraft Wiki API**: для получения рецептов крафта.
- **Ollama**: для генерации стратегий выживания ("Как выжить в Нижнем мире с 10 HP?").

> Интеграция в разработке. Доступна через `ai/` модули.

---

### 🔄 Авто-управление версиями

- Скрипт `tools/update_versions.js` может **обновлять список версий** из интернета.
- Скрипт `parse_and_create_version_folders_only.py` **создаёт папки** для всех версий из `version.txt`.
- При запуске `index.js` проверяется, поддерживается ли указанная версия.

---

### 📁 Папка `versions/`

- Здесь создаются **отдельные папки** под каждую версию Minecraft.
- В каждой папке можно разместить **уникальный `bot.js`** для этой версии.
- Если `bot.js` не найден, он **автоматически создаётся** из `templates/bot_template.js`.

---

### 📚 Пример `bot.js` для версии

```js
/**
 * Шаблон бота для версии 1.21.11
 */

module.exports = (bot) => {
  console.log(`[ULTRABOT v1.21.11] Бот запущен.`);

  // Пример: бот нашёл алмаз
  bot.world.onFindOre = (x, y, z) => {
    bot.swarm.onFindOre(x, y, z);
  };

  // Пример: обучение от игрока
  bot.learner.learnCraftingFromPlayer = (player, recipe) => {
    console.log(`🤖 Изучаю крафт: ${recipe} от ${player}`);
  };
};
```

---

### 🛡️ Поддержка offline-аккаунтов

- API **работает с пиратскими серверами** (`auth: 'offline'`).
- Автоматически отправляет `/register` и `/login`.

---

### 🧰 Инструменты

- `tools/update_versions.js`: Обновляет список версий из интернета.
- `parse_and_create_version_folders_only.py` (Python): Парсит `version.txt` и создаёт папки `versions/<version>/`.

---

### 📝 Лицензия

MIT

---

### 👨‍💻 Автор

Хозяин

---
